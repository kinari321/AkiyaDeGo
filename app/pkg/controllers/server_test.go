package controllers

import (
	"testing"
)

func TestStartMainServer(t *testing.T) {
	// var err error
	// want := err
	// got := StartMainServer()
	// if got == err {
	// 	t.Errorf("Test Failed: got %v, want %v", got, want)
	// }
}
